# Algora Audience Engine: MVP Architecture (T1 — Селлеры маркетплейсов)

## 1. Обзор системы

```
┌─────────────────────────────────────────────────────────────────┐
│                    ALGORA AUDIENCE ENGINE                        │
│                                                                 │
│  ┌───────────┐    ┌───────────┐    ┌───────────┐    ┌────────┐ │
│  │  COLLECT   │───>│  ANALYZE   │───>│  COMPOSE   │───>│ PUBLISH│ │
│  │  (Парсеры) │    │  (ИИ-ядро) │    │ (Контент)  │    │  (TG)  │ │
│  └───────────┘    └───────────┘    └───────────┘    └────────┘ │
│        │                │                │               │      │
│        v                v                v               v      │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    STORAGE (SQLite)                       │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

**4 модуля, работающих последовательно (pipeline):**

| Модуль | Вход | Выход | Задача |
|--------|------|-------|--------|
| **Collect** | Китайские площадки | Сырые данные товаров | Парсинг трендовых товаров |
| **Analyze** | Сырые данные | Оценённые товары | ИИ фильтрует, считает маржу, ранжирует |
| **Compose** | Оценённые товары | Готовый пост | ИИ генерирует текст поста для Telegram |
| **Publish** | Готовый пост | Опубликованный пост | Отправка в Telegram-канал через бота |

## 2. Модуль COLLECT (Сбор данных)

### 2.1 Источники данных (приоритет)

| Приоритет | Источник | Тип данных | Сложность |
|-----------|----------|------------|-----------|
| 1 | **1688.com** | Оптовые цены, объёмы продаж, фабрики | Средняя (китайский, антибот) |
| 2 | **Pinduoduo/Temu** | Розничные тренды, рейтинги | Средняя |
| 3 | **WB/Ozon аналитика** | Конкуренция, цены в РФ, спрос | Низкая (есть API) |
| 4 | **Google Trends / Baidu Index** | Поисковые тренды | Низкая |

### 2.2 Что собираем по каждому товару

```python
@dataclass
class RawProduct:
    source: str              # откуда (1688, pinduoduo, etc.)
    source_url: str          # ссылка на товар
    title_cn: str            # название на китайском
    title_ru: str            # перевод на русский
    category: str            # категория
    price_cny: float         # цена в юанях (FOB/оптовая)
    min_order: int           # минимальный заказ
    sales_volume: int        # объём продаж за 30 дней
    sales_trend: float       # % роста продаж (30d vs предыдущие 30d)
    rating: float            # рейтинг товара
    supplier_name: str       # название поставщика
    supplier_years: int      # лет на площадке
    images: list[str]        # URL изображений
    specs: dict              # технические характеристики
    collected_at: datetime   # дата сбора
```

### 2.3 Подход к парсингу для MVP

Для MVP используем **гибридный подход**:
- **Основной:** HTTP-запросы + BeautifulSoup/parsel для 1688.com
- **Запасной:** Playwright (headless browser) для страниц с JS-рендерингом
- **Перевод:** API перевода (DeepL или Google Translate) для китайского текста
- **Антибот:** Ротация User-Agent, задержки между запросами, прокси при необходимости

## 3. Модуль ANALYZE (Анализ и ранжирование)

### 3.1 Автоматические метрики (без ИИ)

```python
@dataclass
class AnalyzedProduct(RawProduct):
    # Расчётные метрики
    price_rub: float           # цена в рублях (по курсу ЦБ)
    delivery_cost_est: float   # оценка доставки (по весу/категории)
    customs_duty_est: float    # оценка таможенной пошлины
    total_landed_cost: float   # полная себестоимость в РФ
    wb_avg_price: float        # средняя цена на WB в этой категории
    wb_competitors: int        # количество конкурентов на WB
    margin_pct: float          # расчётная маржа (%)
    margin_rub: float          # расчётная маржа (руб)

    # Скоринг
    trend_score: float         # 0-10 (рост тренда)
    competition_score: float   # 0-10 (мало конкурентов = высокий)
    margin_score: float        # 0-10 (высокая маржа = высокий)
    reliability_score: float   # 0-10 (надёжность поставщика)
    total_score: float         # итоговый балл
```

### 3.2 ИИ-анализ (Claude API)

ИИ получает топ-N товаров по скорингу и дополняет анализ:
- **Интерпретация тренда:** почему этот товар растёт? сезонность? вирусный эффект?
- **Риски:** потенциальные проблемы (сертификация, хрупкость, конкуренты уже завозят)
- **Actionable insight:** конкретная рекомендация для селлера

## 4. Модуль COMPOSE (Генерация контента)

### 4.1 Формат поста в Telegram

```
🔍 ALGORA | Находка дня

📦 [Название товара на русском]
📂 Категория: [категория]

💰 Цена FOB: ¥XX (~XX₽)
📦 Мин. заказ: XX шт
🚚 Себестоимость в РФ (ориентир): ~XX₽/шт

📊 Аналитика:
• Тренд в Китае: +XX% за месяц (📈)
• На WB/Ozon: XX конкурентов, средняя цена XX₽
• Расчётная маржа: ~XX%

💡 Инсайт:
[2-3 предложения от ИИ: почему этот товар интересен,
какие риски, для какого типа селлера подходит]

🏭 Поставщик: [имя], [город], [лет на площадке]
🔗 Источник: [ссылка]

#тренды #[категория] #китай
```

### 4.2 Генерация через Claude API

- Промпт включает: данные товара + контекст рынка + tone of voice
- Tone of voice: деловой, конкретный, без воды, с цифрами
- Ограничение: 1 пост = максимум 1500 символов (оптимально для TG)
- ИИ НЕ выдумывает цифры — только интерпретирует предоставленные данные

## 5. Модуль PUBLISH (Публикация)

### 5.1 Telegram Bot

- Бот создаётся через @BotFather
- Бот добавляется как администратор в канал
- Публикация через Telegram Bot API (`sendMessage` / `sendPhoto`)
- Поддержка: текст + 1 изображение товара

### 5.2 Логика публикации

- 1-2 поста в день (не спамим)
- Публикация в оптимальное время (утро 9:00 МСК или вечер 19:00 МСК)
- Дедупликация: не публикуем товар, который уже был в канале (проверка по source_url)

## 6. Storage (Хранилище)

### 6.1 SQLite (для MVP достаточно)

Таблицы:
- `raw_products` — все собранные товары
- `analyzed_products` — товары после анализа
- `published_posts` — опубликованные посты (для дедупликации и аналитики)
- `config` — настройки (курсы валют, пороги скоринга, и т.д.)

### 6.2 Почему SQLite

- Нет зависимостей (не нужен сервер БД)
- Работает локально
- При переходе на сервер — легко мигрировать на PostgreSQL

## 7. Структура проекта

```
algora/
├── README.md
├── PROJECT_MANIFESTO.md
├── SEGMENT_SCORING.md
├── MVP_ARCHITECTURE.md
├── pyproject.toml              # зависимости (Poetry/pip)
├── .env.example                # шаблон переменных окружения
├── src/
│   ├── __init__.py
│   ├── config.py               # конфигурация, переменные окружения
│   ├── models.py               # dataclasses (RawProduct, AnalyzedProduct, etc.)
│   ├── db.py                   # SQLite: создание таблиц, CRUD
│   ├── collect/
│   │   ├── __init__.py
│   │   ├── base.py             # абстрактный класс парсера
│   │   ├── alibaba_1688.py     # парсер 1688.com
│   │   └── wb_analytics.py     # данные о конкуренции на WB
│   ├── analyze/
│   │   ├── __init__.py
│   │   ├── scoring.py          # автоматический скоринг (маржа, тренд)
│   │   ├── enrichment.py       # обогащение данных (курс, доставка)
│   │   └── ai_analysis.py      # Claude API для интерпретации
│   ├── compose/
│   │   ├── __init__.py
│   │   └── telegram_post.py    # генерация текста поста
│   └── publish/
│       ├── __init__.py
│       └── telegram_bot.py     # отправка в Telegram
├── scripts/
│   ├── run_pipeline.py         # запуск полного пайплайна
│   └── setup_db.py             # инициализация БД
├── data/
│   └── algora.db               # SQLite база (gitignored)
└── tests/
    └── ...
```

## 8. Зависимости (Python)

```
# Парсинг
httpx                  # HTTP-клиент (async)
beautifulsoup4         # парсинг HTML
parsel                 # CSS/XPath селекторы
playwright             # headless browser (запасной вариант)

# ИИ
anthropic              # Claude API

# Перевод
deep-translator        # бесплатный перевод (Google/DeepL)

# Telegram
python-telegram-bot    # Telegram Bot API

# Данные
sqlite3                # встроен в Python

# Утилиты
python-dotenv          # переменные окружения
pydantic               # валидация данных
loguru                 # логирование
```

## 9. Переменные окружения (.env)

```
# Claude API
ANTHROPIC_API_KEY=sk-ant-...

# Telegram
TELEGRAM_BOT_TOKEN=123456:ABC...
TELEGRAM_CHANNEL_ID=@algora_trends

# Опционально
PROXY_URL=                    # прокси для китайских сайтов
DEEPL_API_KEY=                # если нужен DeepL вместо Google Translate
```

## 10. Pipeline: Порядок выполнения

```python
# scripts/run_pipeline.py (упрощённая логика)

async def main():
    # 1. Collect: парсим товары из Китая
    raw_products = await collect_trending_products(
        source="1688",
        categories=["electronics", "gadgets", "home"],
        limit=50
    )

    # 2. Analyze: считаем метрики + скоринг
    analyzed = analyze_and_score(raw_products)
    top_products = sorted(analyzed, key=lambda p: p.total_score, reverse=True)[:5]

    # 3. Compose: генерируем пост для лучшего товара
    best = top_products[0]
    post = await compose_telegram_post(best)

    # 4. Publish: отправляем в канал
    await publish_to_telegram(post)

    # 5. Save: сохраняем всё в БД
    save_to_db(raw_products, analyzed, post)
```

## 11. MVP Scope — что делаем, что НЕ делаем

### Делаем (MVP):
- Парсер 1688.com (1 источник)
- Базовая аналитика WB (конкуренция, цены)
- Автоматический скоринг товаров
- ИИ-генерация 1 поста в день
- Публикация в Telegram-канал
- SQLite хранение данных

### НЕ делаем (пока):
- Несколько источников (Pinduoduo, Taobao) — добавим после отладки
- Web-интерфейс или дашборд
- Автоматический запуск по расписанию (cron)
- Обработка изображений (скачивание, ресайз)
- Аналитика подписчиков канала
- Монетизация
